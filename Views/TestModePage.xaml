<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiApp.ViewModels"
             xmlns:models="clr-namespace:MauiApp.Models"
             x:Class="MauiApp.Views.TestModePage"
             Title="테스트 모드"
             x:DataType="vm:TestModeViewModel">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- 헤더 -->
            <Frame BackgroundColor="{StaticResource Primary}" 
                   Padding="15"
                   CornerRadius="10"
                   HasShadow="True">
                <VerticalStackLayout Spacing="5">
                    <Label Text="🧪 테스트 모드"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                    <Label Text="개발 및 디버깅 전용"
                           FontSize="13"
                           TextColor="White"
                           HorizontalOptions="Center"
                           Opacity="0.8"/>
                </VerticalStackLayout>
            </Frame>

            <!-- 서비스 상태 -->
            <Frame BorderColor="{StaticResource Primary}" 
                   HasShadow="True"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📊 서비스 상태" 
                           FontSize="18"
                           FontAttributes="Bold"/>
                    
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="얼굴 감지 서비스" 
                               FontSize="14"
                               VerticalOptions="Center"/>
                        <Label Grid.Column="1"
                               Text="{Binding IsServiceInitialized, Converter={StaticResource BoolToStatusConverter}}"
                               FontSize="14"
                               TextColor="{Binding IsServiceInitialized, Converter={StaticResource BoolToColorConverter}}"
                               FontAttributes="Bold"/>
                    </Grid>

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="감지 횟수" 
                               FontSize="14"
                               VerticalOptions="Center"/>
                        <Label Grid.Column="1"
                               Text="{Binding DetectionCount}"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- 빠른 테스트 -->
            <Frame BorderColor="{StaticResource Primary}" 
                   HasShadow="True"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="15">
                    <Label Text="⚡ 빠른 테스트" 
                           FontSize="18"
                           FontAttributes="Bold"/>

                    <!-- 보호 동작 선택 -->
                    <Label Text="보호 동작 선택:" FontSize="14"/>
                    <Picker x:Name="ActionPicker"
                            SelectedIndex="0"
                            FontSize="14">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>블러 효과</x:String>
                                <x:String>화면 가리기</x:String>
                                <x:String>위장 화면</x:String>
                                <x:String>진동만</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>

                    <!-- 위장 화면 선택 -->
                    <Label Text="위장 화면 선택:" FontSize="14"/>
                    <Picker x:Name="DisguisePicker"
                            SelectedIndex="0"
                            FontSize="14">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>뉴스</x:String>
                                <x:String>주식 시장</x:String>
                                <x:String>전자책</x:String>
                                <x:String>계산기</x:String>
                                <x:String>캘린더</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>

                    <!-- 시뮬레이션 버튼 -->
                    <Button Text="🎭 엿보기 감지 시뮬레이션"
                            Command="{Binding SimulatePeekingCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            HeightRequest="50"/>

                    <Button Text="🛑 보호 해제"
                            Command="{Binding DeactivateProtectionCommand}"
                            BackgroundColor="{StaticResource Secondary}"
                            TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- 위장 화면 미리보기 -->
            <Frame BorderColor="{StaticResource Primary}" 
                   HasShadow="True"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="15">
                    <Label Text="👁️ 위장 화면 미리보기" 
                           FontSize="18"
                           FontAttributes="Bold"/>

                    <Grid ColumnDefinitions="*,*" 
                          RowDefinitions="Auto,Auto,Auto"
                          ColumnSpacing="10"
                          RowSpacing="10">
                        
                        <Button Text="📰 뉴스"
                                Command="{Binding PreviewDisguiseCommand}"
                                CommandParameter="News"
                                BackgroundColor="#1a73e8"
                                TextColor="White"/>
                        
                        <Button Grid.Column="1"
                                Text="📈 주식"
                                Command="{Binding PreviewDisguiseCommand}"
                                CommandParameter="StockMarket"
                                BackgroundColor="#0d1117"
                                TextColor="White"/>
                        
                        <Button Grid.Row="1"
                                Text="📖 전자책"
                                Command="{Binding PreviewDisguiseCommand}"
                                CommandParameter="EBook"
                                BackgroundColor="#8b7355"
                                TextColor="White"/>
                        
                        <Button Grid.Row="1" Grid.Column="1"
                                Text="🧮 계산기"
                                Command="{Binding PreviewDisguiseCommand}"
                                CommandParameter="Calculator"
                                BackgroundColor="Black"
                                TextColor="White"/>
                        
                        <Button Grid.Row="2" Grid.ColumnSpan="2"
                                Text="📅 캘린더"
                                Command="{Binding PreviewDisguiseCommand}"
                                CommandParameter="Calendar"
                                BackgroundColor="#4285f4"
                                TextColor="White"/>
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- 서비스 테스트 -->
            <Frame BorderColor="{StaticResource Primary}" 
                   HasShadow="True"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="🔧 서비스 테스트" 
                           FontSize="18"
                           FontAttributes="Bold"/>

                    <Button Text="📷 카메라 권한 테스트"
                            Command="{Binding TestCameraPermissionCommand}"
                            BackgroundColor="{StaticResource Tertiary}"
                            TextColor="White"/>

                    <Button Text="🤖 얼굴 감지 테스트"
                            Command="{Binding TestFaceDetectionCommand}"
                            BackgroundColor="{StaticResource Tertiary}"
                            TextColor="White"/>

                    <Button Text="🎬 모든 동작 테스트"
                            Command="{Binding TestAllActionsCommand}"
                            BackgroundColor="{StaticResource Tertiary}"
                            TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- 테스트 로그 -->
            <Frame BorderColor="{StaticResource Gray300}" 
                   HasShadow="True"
                   Padding="15"
                   CornerRadius="10"
                   BackgroundColor="{StaticResource Gray100}">
                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="📝 테스트 로그" 
                               FontSize="18"
                               FontAttributes="Bold"/>
                        <Button Grid.Column="1"
                                Text="🗑️ 초기화"
                                Command="{Binding ClearLogCommand}"
                                BackgroundColor="{StaticResource Gray500}"
                                TextColor="White"
                                Padding="10,5"/>
                    </Grid>

                    <ScrollView HeightRequest="200">
                        <Label Text="{Binding TestLog}"
                               FontSize="12"
                               FontFamily="Courier"
                               TextColor="{StaticResource Gray900}"
                               LineBreakMode="WordWrap"/>
                    </ScrollView>
                </VerticalStackLayout>
            </Frame>

            <!-- 뒤로 가기 -->
            <Button Text="← 메인으로 돌아가기"
                    Command="{Binding BackCommand}"
                    BackgroundColor="{StaticResource Gray500}"
                    TextColor="White"
                    Margin="0,10,0,20"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

